import Slider from"./modules/glide";import ApiService from"./modules/api-service";import prepareDate from"./modules/prepare-data";import activateTemplate from"./modules/activate-template";import getRandomCard from"./modules/get-random-card";const slider=document.querySelector(".slider"),loader=document.querySelector(".loader");function renderCards(value){const api=new ApiService;api.getResource(value).then(data=>{const container=new DocumentFragment,response=api.getId(data);response.then(res=>{const buildData=res.map(el=>prepareDate(el).cloneNode(!0));container.appendChild(buildData),console.log(container)})})}renderCards(getRandomCard());const searchForm=document.querySelector(".search-form"),formField=searchForm.querySelector(".search-form__field");searchForm.addEventListener("submit",evt=>{evt.preventDefault();const{value:value}=formField});